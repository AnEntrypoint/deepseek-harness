/**
 * Pure types of the title domain: this module previously carried only a
 * compile-time `SessionProjectionMap`/`SessionProjectionStateMap`
 * augmentation for the `title` projection key — zero runtime representation.
 * Converted to buildless JS: the type-only augmentation is intentionally
 * dropped (no runtime equivalent exists for a `declare module` merge).
 *
 * @module @deepseek-ai/dsh-session-title/types
 */

export {}
